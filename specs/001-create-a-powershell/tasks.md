# Tasks: Download Spec Kit Templates (PowerShell)# Tasks: Download Spec Kit Templates (PowerShell)



**Input**: `specs/001-create-a-powershell/spec.md`**Input**: `specs/001-create-a-powershell/spec.md`

**Feature Branch**: `001-create-a-powershell`**Feature Branch**: `001-create-a-powershell`



## Tasks (T001...)## Execution Flow

1. Setup project artifacts and linting

T001 Setup: Script file and quickstart2. Create failing tests (Pester) for expected behavior (TDD)

- Create: `tools/spec-kit-downloader.ps1` (script entrypoint)3. Implement core script (download, validate, extract)

- Create: `specs/001-create-a-powershell/quickstart.md` (usage)4. Implement helpers (retry, zip validation, safe-extract)

5. Add CI: lint + tests

T002 Lint config6. Manual validation and quickstart

- Create: `.psscriptanalyzer.psd1` (minimal rules)7. Docs, polish, PR

- Create: `.gitattributes` for line endings

## Tasks (T001...)

T003 [P] Unit tests - arg parsing

- Create: `tests/powershell/unit/01-arguments.Tests.ps1` to assert parameters and defaultsT001 Setup: Create script skeleton and project files

- Path: `tools/spec-kit-downloader.ps1` (create this file)

T004 [P] Unit tests - asset selection- Create: param block, comment-based help (Synopsis, Description, Parameters, Examples), placeholder functions: Get-LatestReleaseTag, Find-ReleaseAsset, Download-Asset, Validate-Zip, Extract-Zip

- Create: `tests/powershell/unit/02-asset-selection.Tests.ps1` to mock GitHub responses and assert asset selection- Create: `specs/001-create-a-powershell/quickstart.md` with intended usage examples

- Rationale: Provides a clear starting point and CI targets

T005 Core: release discovery + asset selection

- Implement Get-LatestReleaseTag and Find-ReleaseAsset in `tools/spec-kit-downloader.ps1`T002 [P] Linting & style configuration

- Path: `.psscriptanalyzer.psd1` (repo root)

T006 Core: download + retry helper- Create: minimal PSScriptAnalyzer ruleset enabling recommended rules for modules/scripts; include a baseline exclusion section for generated files

- Implement Download-Asset with retry and optional Authorization header- Path: `.gitattributes` (repo root) to normalize line endings for PowerShell files

- Rationale: Enforce style and avoid cross-platform line-ending issues

T007 Core: validate ZIP + safe extract

- Implement Validate-Zip and Safe-Extract with skip/--force behaviorT003 [P] Tests - Argument parsing and validation (Pester)

- Path: `tests/powershell/unit/01-arguments.Tests.ps1`

T008 Interactive & agent auto-selection- Create Pester tests that assert:

- Implement auto-select heuristics and `--interactive` prompt  - The script exposes the required parameters (`--agent`, `--shell`, `--version`, `--retry`, `--force`, `--path`, `--interactive`)

  - Default behaviors when parameters omitted (agent auto-select, shell=ps, retry=3)

T009 Tests - integration- Tests should be written to fail initially (TDD)

- Create: `tests/powershell/integration/01-download-integration.Tests.ps1` (recorded HTTP responses or use a stub)

T004 [P] Tests - Asset selection & fallback behavior (Pester)

T010 CI: GitHub Actions workflow- Path: `tests/powershell/unit/02-asset-selection.Tests.ps1`

- Create: `.github/workflows/powershell-lint-test.yml` to run PSScriptAnalyzer + Pester- Create tests that mock GitHub API responses and verify:

  - Correct selection of latest semver tag

T011 Docs & PR  - Correct asset matching for `spec-kit-template-[agent]-[ps|sh]-v[version].zip`

- Update README and prepare PR with clarifications listed  - Behavior when asset missing (error path)

- Tests must fail before implementation

## Ordering

- Setup (T001/T002) → Tests (T003/T004) → Core (T005-T008) → Integration Tests (T009) → CI (T010) → Docs/PR (T011)T005 Core Implementation: Release discovery + selection

- Path: `tools/spec-kit-downloader.ps1` (implement)

- Implement Get-LatestReleaseTag: query GitHub releases/tags (use GitHub API unauthenticated or with `GITHUB_TOKEN` if present)

---- Implement Find-ReleaseAsset: locate asset by pattern and requested shell; prefer highest semver tag

- Write clear error codes for not-found or permission issues

Generated by /tasks- Rationale: core discovery logic


T006 Core Implementation: Download + retry helper
- Path: `tools/spec-kit-downloader.ps1` (same file)
- Implement Download-Asset using Invoke-RestMethod / Invoke-WebRequest with optional Authorization header from `GITHUB_TOKEN`
- Implement retry logic: default 3 retries with exponential backoff; allow `--retry` override
- Save downloaded file to a temporary path

T007 Core Implementation: Validate ZIP + safe extraction
- Path: `tools/spec-kit-downloader.ps1`
- Implement Validate-Zip: open with [System.IO.Compression.ZipFile] and catch exceptions
- Implement Extract-Zip: extract to a temporary folder then move files to target path; skip existing files by default, support `--force` to overwrite; support `--path <dir>` override
- Ensure no half-extracted state on failure (cleanup temp files)

T008 Core Implementation: Interactive & agent auto-selection
- Path: `tools/spec-kit-downloader.ps1`
- Implement agent auto-selection when `--agent` omitted (choose latest/popular from release metadata); add `--interactive` mode to prompt the user before downloading when ambiguous

T009 Logging, exit codes, and help
- Path: `tools/spec-kit-downloader.ps1`
- Add structured logging to stderr/stdout with clear messages
- Define exit codes for success (0), generic failure (1), network failure (2), asset not found (3), permission/auth (4)
- Ensure comment-based help examples in the script are accurate

T010 Tests - Integration scenario (Pester)
- Path: `tests/powershell/integration/01-download-integration.Tests.ps1`
- Create one integration test that runs the script against a stubbed or sample release (or uses recorded responses) and asserts files extracted to `--path` or cwd
- Mark as [P] if tests are independent and do not write to shared files

T011 CI: Lint and Test workflow
- Path: `.github/workflows/powershell-lint-test.yml`
- Create GitHub Actions workflow that runs on pull_request and runs:
  - Setup PowerShell (actions/setup-powershell)
  - Install Pester
  - Run PSScriptAnalyzer using `.psscriptanalyzer.psd1`
  - Run Pester unit and integration tests
- Rationale: enforce gates per constitution

T012 Docs & Quickstart
- Path: `specs/001-create-a-powershell/quickstart.md` (update)
- Path: `tools/README.md` or repo `README.md` (update usage section)
- Document examples: default run, `--path`, `--force`, `--shell sh`, `GITHUB_TOKEN` usage

T013 Manual verification & smoke test
- Steps:
  1. Run `pwsh tools/spec-kit-downloader.ps1` in an empty temp directory
  2. Verify exit code 0 and files extracted
  3. Run `pwsh tools/spec-kit-downloader.ps1 --shell sh --path ./tmp` and verify behavior
- Add these steps to `specs/001-create-a-powershell/quickstart.md`

T014 Release / PR prep
- Create a PR branch (already on `001-create-a-powershell`) and include:
  - Script, tests, quickstart, CI workflow
  - Summary of clarifications and decisions in PR description

## Parallel execution examples
- Run unit tests in parallel (T003 & T004 & T010) since they target different files
- Linting (T002) and tests (T003/T004) can run in parallel in CI

## Ordering & Dependencies
- Setup (T001/T002) before Tests (T003/T004)
- Tests (T003/T004) must exist and fail before Core Implementation (T005-T009)
- Core Implementation helpers (T006-T007) should be authored alongside T005/T008
- CI (T011) added early so PRs run checks
- Docs (T012) and manual smoke (T013) after implementation


---

Generated by `/tasks` for feature: Download Spec Kit Templates (PowerShell)
